/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['mir-s3-cdn-cf.behance.net','m.media-amazon.com','www.thenaturalvillage.com','encrypted-tbn0.gstatic.com'], // Add the hostname here
      },
};

export default nextConfig;
